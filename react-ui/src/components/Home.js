
import { withRouter } from 'react-router-dom';

import React, { Component }  from 'react';

function Home(props)
{


    return (
        <div>
            <h2> Express - React for neural network</h2>
        </div>
    );

}

export default withRouter(Home);